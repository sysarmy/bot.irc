services:
  bot:
    build: .
    user: "${PUID:-1000}:${PGID:-1000}"
    env_file:
      - .env
    volumes:
      - ./main.py:/app/main.py:ro
      - ./src:/app/src:ro
      - ./db:/app/db
    restart: unless-stopped
